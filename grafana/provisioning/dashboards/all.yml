#- name: 'default'
#  org_id: 1
#  folder: ''
#  type: 'file'
#  options:
#    folder: '/var/lib/grafana/dashboards'

apiVersion: 1

providers:
  - name: Default # A uniquely identifiable name for the provider
    folder: Services # The folder where to place the dashboards
    type: file
    options:
      path: /etc/grafana/dashboards/blackbox.yml
